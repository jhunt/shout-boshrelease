#!/bin/bash
set -eu

cpus=$(grep -c ^processor /proc/cpuinfo)

tar -xjvf sbcl/sbcl-1.4.8.tar.bz2
pushd sbcl-1.4.8
  apt-get install -y sbcl # strictly for bootstrapping
  ./make.sh --fancy --prefix=$BOSH_INSTALL_TARGET
  ./install.sh
popd
